import {MESSAGE_TYPE} from '../conversation.enum';
import {MessageModel} from '../models';

// tslint:disable-next-line:no-namespace
export namespace MessageDTO {
    export function toJSON(message: any, user?: any): MessageModel {

    }
}
